### insert sort
```  
插入排序：e.g. 递增
将数组前面有序的和后面无序的两段，初始时有序只有arr[0], 无序有arr[1...n-1];
每次将无序的第一个元素与前面有序的进行比较插入到递增的位置, 这样有序:arr[0..1], 无序:arr[2...n-1];
同理处理后续。某些算法将arr[0]作为哨兵。arr的实际数据从1到n。
自己可以按照实际情况来完成插入到有序数组应该的位置。
```  
